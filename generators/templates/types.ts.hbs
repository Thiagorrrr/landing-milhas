import {{pascalCase name}} from '@mui/material/{{pascalCase name}}'
import { css } from 'styled-components'

export interface {{pascalCase name}}Props extends BaseProps<typeof css> {
    // add your custom attributes
    {{pascalCase name}}:string
}